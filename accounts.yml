server:
  port: 9010
spring:
  datasource:
    url: jdbc:postgresql://localhost:5432/accounts_ms
    driver-class-name: org.postgresql.Driver
    username: postgres
    password: Ziyad1917
  jpa:
    database-platform: org.hibernate.dialect.PostgreSQLDialect
    hibernate:
      ddl-auto: update
    show-sql: true

info:
  app:
    name: "accounts"
    description: "bank project Account Application"
    version : "1.0.0"


